{{ include('base.html.twig') }}

{% block stylesheets %}
    <style>
        .col-sm-2{
            min-width: 200px;
        }
        .align-items-center{
            max-width: 1026px;
        }
    </style>
{% endblock %}

{% block editar %}
    <div class="container mt-xxl-5">
        <div class="card border-dark" style="width: fit-content">
            <div class="card-body text-dark">
                <div class="card-title"><h2>{{ titulo }} com identificador #{{ id }}</h2></div>
                {{ form(formulario) }}
            </div>
            {{ include('includes/alerts.html.twig') }}
        </div>
    </div>

{% endblock %}