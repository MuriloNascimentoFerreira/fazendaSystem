{% extends 'homePage.html.twig' %}
{% block stylesheets %}
<style>
    .col-sm-2{
        min-width: 200px;
    }
    .align-items-center{
        max-width: 1026px;
    }
</style>
{% endblock %}

{% block formulario %}
    <div class="container mt-1">
        <div class="card border-dark" style="width: fit-content">

            <div class="card-body text-dark">
                <div class="card-title"><h2 class="d-inline-block">{{ titulo }}</h2> <div class="d-inline-block" style="font-size: 1.5rem"> #{{ id }}</div></div>
                {{ form(form) }}
            </div>
            {{ include('includes/alerts.html.twig') }}
        </div>
        {{ include('animal/lista.html.twig',animais) }}
    </div>

{% endblock %}
